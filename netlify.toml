[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  NETLIFY_BUILD_IMAGE = "noble"

# Enable Next.js runtime
[[plugins]]
  package = "@netlify/plugin-nextjs"
